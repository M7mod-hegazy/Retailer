# NEVER commit issued-license data or generated artifacts.
registry.json
out/

# The private key lives in ~/.retailer-keys, never here — but guard anyway.
*.pem
license-private*

# Standalone app build artifacts
node_modules/
dist/
package-lock.json
